apply plugin: 'java'
apply plugin: 'eclipse'

buildscript {
    repositories {
        mavenCentral()
    }
}

version = '0.0.1-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':backend')
//    include ('frontend/dist')
}

